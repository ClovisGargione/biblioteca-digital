import { store as auth } from '@/modules/auth'
import { store as registrar } from '@/modules/registrar'
import { store as livro } from '@/modules/livro'

export default {
    auth,
    registrar,
    livro
}